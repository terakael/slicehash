---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/slicehash/__init__.py
  - src/slicehash/config.py
  - config.yaml
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "Configuration file exists with billable threshold, pool URL, and database path"
    - "Configuration can be loaded and validated at runtime"
    - "Project dependencies are managed via uv"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata and dependencies (Quart, aiosqlite, pydantic)"
      contains: "quart"
    - path: "config.yaml"
      provides: "Runtime configuration values"
      contains: "billable_difficulty_threshold"
    - path: "src/slicehash/config.py"
      provides: "Configuration loader with validation"
      exports: ["Config", "load_config"]
  key_links:
    - from: "src/slicehash/config.py"
      to: "config.yaml"
      via: "YAML file loading"
      pattern: "yaml\\.safe_load"
---

<objective>
Establish Python project structure and configuration layer for the SliceHash mining backend.

Purpose: Foundation for all subsequent development - defines project dependencies, configuration schema, and runtime settings management.
Output: Working Python project with validated configuration loading.
</objective>

<execution_context>
@/home/dan/.claude/get-shit-done/workflows/execute-plan.md
@/home/dan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/dan/git/personal/slicehash/.planning/PROJECT.md
@/home/dan/git/personal/slicehash/.planning/ROADMAP.md
@/home/dan/git/personal/slicehash/.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Python project with uv</name>
  <files>pyproject.toml, src/slicehash/__init__.py, .gitignore</files>
  <action>
Initialize a new Python project using uv with the following structure:
- Project name: slicehash
- Python version: >=3.11
- Source layout: src/slicehash/

Add dependencies:
- quart (async web framework)
- aiosqlite (async SQLite driver)
- pydantic (configuration validation)
- pyyaml (config file parsing)

Create .gitignore with:
- __pycache__/
- *.py[cod]
- .venv/
- *.db
- *.sqlite3

Use uv commands (not pip). Create virtual environment and ensure dependencies install correctly.
  </action>
  <verify>
Run `uv sync` to install dependencies. Verify pyproject.toml contains all required packages. Check that src/slicehash/__init__.py exists (can be empty).
  </verify>
  <done>
pyproject.toml exists with Quart, aiosqlite, pydantic, pyyaml dependencies. Virtual environment created. Project structure follows src layout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create configuration module with validation</name>
  <files>src/slicehash/config.py</files>
  <action>
Create src/slicehash/config.py with:
1. Pydantic model `Config` defining:
   - billable_difficulty_threshold: float
   - pool_url: str (HTTP URL)
   - database_path: str (file path)

2. Function `load_config(path: str = "config.yaml") -> Config`:
   - Load YAML file
   - Validate with Pydantic
   - Return Config instance
   - Raise clear error if file missing or invalid

Use Pydantic v2 syntax. Include type hints. Add docstrings.
  </action>
  <verify>
Create test script that imports load_config and verifies it raises error when config.yaml doesn't exist. Check that Config model validates field types.
  </verify>
  <done>
src/slicehash/config.py exists with Config model and load_config function. Type validation works. Clear error messages on invalid input.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create default configuration file</name>
  <files>config.yaml, config.example.yaml</files>
  <action>
Create config.yaml with sensible defaults:
```yaml
# SliceHash Mining Backend Configuration

# Difficulty threshold for classifying shares as billable
# Shares with difficulty >= this value count toward quota
billable_difficulty_threshold: 1000000.0

# URL of the SV2 pool for coinbase address updates
pool_url: "http://localhost:8080"

# Path to SQLite database file
database_path: "slicehash.db"
```

Create config.example.yaml as a copy with comments explaining each field.

Add config.yaml to .gitignore (contains deployment-specific values).
Add config.example.yaml to git (template for users).
  </action>
  <verify>
Run `python -c "from slicehash.config import load_config; c = load_config(); print(c.billable_difficulty_threshold)"` from src parent directory. Should print 1000000.0.
  </verify>
  <done>
config.yaml exists with all required fields. config.example.yaml provides documented template. Configuration loads successfully via load_config().
  </done>
</task>

</tasks>

<verification>
1. Run `uv sync` - dependencies install without errors
2. Run configuration loading test - config.yaml parses correctly
3. Check pyproject.toml contains quart, aiosqlite, pydantic, pyyaml
4. Verify src/slicehash/config.py exports Config and load_config
</verification>

<success_criteria>
- [ ] pyproject.toml defines project with required dependencies
- [ ] Virtual environment can be created with `uv venv`
- [ ] config.yaml contains billable_difficulty_threshold, pool_url, database_path
- [ ] load_config() successfully loads and validates configuration
- [ ] Clear error messages when config is missing or invalid
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
